# 仓库内一律 LF：本插件跨平台分发，而主机侧会把 lib/client.js 作为浏览器 bundle
# 直接投喂给模块系统——CRLF 会跟着进产物。Windows 检出时交由工具侧处理，不要在库内混行尾。
* text=auto eol=lf

# 产物必须与源码行尾一致，否则每次构建都会产生无意义 diff
lib/** text eol=lf
src/** text eol=lf
scripts/** text eol=lf

# 二进制/无需归一化的类型
*.png binary
*.jpg binary
*.ico binary
*.tgz binary
